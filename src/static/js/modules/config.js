const config = {
    debug: {
        maxFullViewItems: 100,  // Maximum number of items allowed in full view
        defaultTruncateSize: 5 // Default number of items to show in truncated view
    },
    ui: {
        alertDuration: 5000,
        autoRefresh: false
    }
};

export default config;
